import template from './template.twig';

const { Mixin } = Shopware;

export default {
    template,

    inject: [
        'repositoryFactory',
        'acl',
    ],

    mixins: [

    ],

    data(): {
        return {

        };
    },

    computed: {

    },

    methods: {

    }
}